Sockets com Thread Pool:

Parametros que devem ser passados ao programa:

-single			//executa uma única thread no pool

-fixed <number>		//executa um threadpool de tamanho n

-cached			//thread pool de tamanho ilimitado